import styled from 'styled-components';
import { StyledErrorContainer } from '../ErrorContainer/ErrorContainer.style';

export const StyledLoadingContainer = styled(StyledErrorContainer)`
  border: 1px solid black;
`;
