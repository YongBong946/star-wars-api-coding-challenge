import styled from 'styled-components';

export const StyledHeaderContainer = styled.div`
  display: flex;
  justify-content: center;
`;

export const StyledBodyContainer = styled(StyledHeaderContainer)``;
